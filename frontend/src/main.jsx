import React from 'react';
import ReactDOM from 'react-dom/client';
import DexterSidebarUI from './DexterSidebarUI.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DexterSidebarUI />
  </React.StrictMode>,
);
